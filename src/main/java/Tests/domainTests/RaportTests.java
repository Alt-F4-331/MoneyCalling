/*package Tests.domainTests;

import Domain.Raport;
import org.junit.jupiter.api.BeforeEach;
import org.junit.jupiter.api.Test;

import static org.junit.jupiter.api.Assertions.assertEquals;

public class RaportTests {
    private Raport raport;

    @BeforeEach
    public void setUp(){
        raport = new Raport(1, 2);
    }

    @Test
    public void testConstructor(){
        assertEquals(1, raport.getId(), "raport id must be 1");
        assertEquals(2, raport.getIdDiagrama(), "raport diagramaid must be 2");
    }

    @Test
    public void testsetDiagrama(){
        raport.setIdDiagrama(3);
        assertEquals(3, raport.getIdDiagrama(), "raport diagramaid must be 3");
    }
}
*/